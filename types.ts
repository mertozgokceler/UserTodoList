export interface Todo {
  id: number
  text: string
  createdDate: string
  endDate: string
  category: string
  isCompleted?: boolean
}

export interface User {
   id:number
   name:string
   mail:string
}
